export class ContentPage {
  title: string;
  parentURL: string;

  constructor (
    title: string,
    parentURL: string,
    ) {
     this.title = title;
      this.parentURL = parentURL;
    }
}

